fastapi
uvicorn  # aysnc web server gateway interface (wsgi)
# astrapy  # Vector : AstraDB : NoSQL database : Cassandra
cassandra-driver # Non Vector Cassandra driver
python-decouple  # manage environment variables
pydantic  # data validation
pydantic-settings  # for importing BaseSettings
python-dotenv  # for importing load_dotenv. We dont see environment variables in terminal, but this package will help us to load them when we run the app.